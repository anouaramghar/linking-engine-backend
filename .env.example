# --- Database (PostgreSQL 16 + pgvector) ---
DATABASE_URL=postgresql+psycopg://linkmesh:linkmesh@localhost:15432/linkmesh

# --- Redis (RQ task queue) ---
REDIS_URL=redis://localhost:6379/0

# --- API ---
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development
# Required X-API-Key header on all non-health endpoints; empty disables auth (local dev only)
API_KEY=

# --- Crawl safety ---
# The crawler refuses private/loopback/link-local/metadata targets and requires
# HTTPS when WP credentials are set. true relaxes both, e.g. for a WordPress
# test site on localhost. Development-only: refused outside ENVIRONMENT=development.
ALLOW_UNSAFE_CRAWL_TARGETS=false

# --- External search (v3 — leave empty until needed) ---
TAVILY_API_KEY=

# --- Embeddings ---
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=cpu
